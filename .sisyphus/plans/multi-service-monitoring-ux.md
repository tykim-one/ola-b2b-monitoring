# Multi-Service Monitoring UX Restructuring Plan

## 개요

OLA B2B Monitoring 시스템을 **멀티 서비스 중심 구조**로 재구성하여, 다양한 서비스(챗봇, ETL, 배치 등)를 직관적이고 확장 가능하게 모니터링할 수 있도록 합니다.

## 현재 상태

### 모니터링 대상 서비스
- **ibk-chat**: 챗봇 품질관리 모니터링 (대화형/LLM)
- **ibk**: 일일 리포트 자동생성, DB 배치 모니터링 (파이프라인)
- **wind-etl**: 파일처리 모니터링 (파이프라인)
- **minkabu-etl**: 뉴스 번역 처리 모니터링 (파이프라인)
- **+ 미래 서비스**: 성격 미정

### 현재 문제점
- PROJECT_ID가 'ibks'로 하드코딩
- 기능 중심 메뉴 구조 (서비스 많아지면 찾기 어려움)
- 홈 대시보드가 단일 서비스(ibks) 데이터만 표시
- 서비스 추가 시 어디에 메뉴를 넣어야 할지 불명확

## 결정된 방향

| 항목 | 결정 |
|------|------|
| **접근 방식** | 컴포넌트 라이브러리 강화 + 조합 |
| **사이드바** | 서비스 중심 그룹핑 |
| **홈 대시보드** | 서비스 카드 모음 (상태 + 3 KPI + 미니차트) |
| **서비스 분류** | 대화형(LLM) / 파이프라인(ETL/배치) + 확장 가능 |
| **구현 전략** | 점진적 접근 (인프라 → 프로토타입 → 확장) |
| **UX 목표** | 챗봇=깊은 분석, 파이프라인=빠른 문제 발견 |

## 아키텍처 설계

### 서비스 타입 정의

```typescript
// packages/shared-types/src/service.types.ts

export type ServiceType = 'chatbot' | 'pipeline' | 'batch' | 'custom';

export interface ServiceConfig {
  id: string;                    // 'ibk-chat', 'wind-etl' 등
  name: string;                  // 표시 이름
  type: ServiceType;             // 서비스 유형
  icon: string;                  // 아이콘 이름
  description: string;           // 설명

  // 홈 카드 설정
  card: {
    kpis: ServiceKPIConfig[];    // 표시할 KPI 3개
    chart: ServiceChartConfig;   // 미니 차트 설정
  };

  // 사이드바 메뉴 설정
  menu: ServiceMenuItem[];       // 서비스 내 메뉴 항목

  // API 엔드포인트 (선택적 오버라이드)
  endpoints?: {
    health?: string;             // 헬스체크 API
    kpi?: string;                // KPI API
    chart?: string;              // 차트 데이터 API
  };
}

export interface ServiceKPIConfig {
  key: string;                   // API 응답 필드명
  label: string;                 // 표시 라벨
  format: 'number' | 'percentage' | 'duration' | 'currency';
  thresholds?: {                 // 상태 판단 기준
    warning?: number;
    error?: number;
  };
}

export interface ServiceChartConfig {
  type: 'line' | 'bar' | 'progress' | 'status-list';
  dataKey: string;
  label: string;
}

export interface ServiceMenuItem {
  id: string;
  label: string;
  path: string;                  // 상대 경로 (서비스 base 기준)
  icon?: string;
}
```

### 서비스 설정 예시

```typescript
// apps/frontend-next/src/config/services.ts

export const serviceConfigs: ServiceConfig[] = [
  {
    id: 'ibk-chat',
    name: 'IBK 챗봇',
    type: 'chatbot',
    icon: 'MessageSquare',
    description: '챗봇 품질관리 모니터링',
    card: {
      kpis: [
        { key: 'activeSessions', label: '활성 세션', format: 'number' },
        { key: 'resolutionRate', label: '해결률', format: 'percentage', thresholds: { warning: 80, error: 60 } },
        { key: 'avgResponseTime', label: '평균 응답', format: 'duration' },
      ],
      chart: { type: 'line', dataKey: 'traffic', label: '실시간 트래픽' }
    },
    menu: [
      { id: 'quality', label: '품질 분석', path: '/quality' },
      { id: 'users', label: '유저 분석', path: '/users' },
      { id: 'ai-performance', label: 'AI 성능', path: '/ai-performance' },
      { id: 'batch-analysis', label: '배치 분석', path: '/batch-analysis' },
    ]
  },
  {
    id: 'wind-etl',
    name: 'Wind ETL',
    type: 'pipeline',
    icon: 'FileText',
    description: '파일처리 모니터링',
    card: {
      kpis: [
        { key: 'processing', label: '처리 중', format: 'number' },
        { key: 'queued', label: '대기', format: 'number' },
        { key: 'successRate', label: '성공률', format: 'percentage', thresholds: { warning: 95, error: 90 } },
      ],
      chart: { type: 'progress', dataKey: 'dailyProgress', label: '오늘 처리량' }
    },
    menu: [
      { id: 'status', label: '처리 현황', path: '/status' },
      { id: 'logs', label: '에러 로그', path: '/logs' },
    ]
  },
  // ... 다른 서비스들
];
```

### 새로운 사이드바 구조

```
🏠 홈 (전체 서비스 헬스)
──────────────────────────
💬 IBK 챗봇
   ├── 품질 분석
   ├── 유저 분석
   ├── AI 성능
   └── 배치 분석
📊 IBK 리포트
   ├── 배치 현황
   └── 데이터 적재
🔄 Wind ETL
   ├── 처리 현황
   └── 에러 로그
🔄 Minkabu ETL
   ├── 번역 현황
   └── 에러 로그
──────────────────────────
⚙️ 시스템 관리
   ├── 사용자
   ├── 역할
   └── 필터
```

### 새로운 홈 대시보드

```
┌──────────────────────────────────────────────────────────────────────────┐
│  🏠 모니터링 홈                                              [새로고침]  │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐       │
│  │ 🟢 IBK 챗봇                 │  │ 🟢 IBK 리포트               │       │
│  │ ───────────────────────────  │  │ ───────────────────────────  │       │
│  │ 활성 세션    해결률    응답   │  │ 마지막 실행   성공률    적재  │       │
│  │    23       87.2%    1.2s   │  │  06:00 ✓     100%     OK    │       │
│  │ [미니 트래픽 차트]           │  │ [적재 상태 리스트]           │       │
│  │                [상세 보기 →]│  │                [상세 보기 →]│       │
│  └─────────────────────────────┘  └─────────────────────────────┘       │
│                                                                          │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐       │
│  │ 🟢 Wind ETL                 │  │ 🟡 Minkabu ETL              │       │
│  │ ───────────────────────────  │  │ ───────────────────────────  │       │
│  │ 처리 중    대기    성공률    │  │ 처리 중    대기    성공률    │       │
│  │    5       12     99.2%    │  │    3       45     94.1%    │       │
│  │ [처리량 프로그레스 바]       │  │ [⚠ 경고 메시지]             │       │
│  │                [상세 보기 →]│  │                [상세 보기 →]│       │
│  └─────────────────────────────┘  └─────────────────────────────┘       │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

## 구현 계획

### Phase 1: 인프라 구축 (1-2일)

#### Task 1.1: 서비스 타입 정의
- **파일**: `packages/shared-types/src/service.types.ts`
- **내용**: ServiceConfig, ServiceType, ServiceKPIConfig 등 타입 정의
- **검증**: 타입 빌드 성공

#### Task 1.2: 서비스 설정 파일 생성
- **파일**: `apps/frontend-next/src/config/services.ts`
- **내용**: 4개 서비스(ibk-chat, ibk, wind-etl, minkabu-etl) 설정
- **검증**: import 및 타입 체크 통과

#### Task 1.3: ServiceCard 컴포넌트 개발
- **파일**: `apps/frontend-next/src/components/service-card/ServiceCard.tsx`
- **내용**:
  - 상태 아이콘 (🟢🟡🔴)
  - 3개 KPI 표시
  - 미니 차트 (line/bar/progress/status-list)
  - 클릭 시 상세 페이지 이동
- **검증**: Storybook 또는 테스트 페이지에서 렌더링 확인

#### Task 1.4: useServiceHealth 훅 개발
- **파일**: `apps/frontend-next/src/hooks/queries/use-service-health.ts`
- **내용**:
  - 서비스별 헬스체크 API 호출
  - KPI 데이터 fetching
  - 상태 계산 로직 (정상/경고/에러)
- **검증**: API 연동 테스트

### Phase 2: ibk-chat 프로토타입 (2-3일)

#### Task 2.1: 홈 대시보드 카드 뷰 구현
- **파일**: `apps/frontend-next/src/app/dashboard/page.tsx`
- **내용**:
  - ServiceCard 그리드 레이아웃
  - 서비스 설정 기반 동적 렌더링
  - 로딩/에러 상태 처리
- **검증**: 4개 서비스 카드가 홈에 표시

#### Task 2.2: 사이드바 서비스 그룹 추가
- **파일**: `apps/frontend-next/src/components/Sidebar.tsx`
- **내용**:
  - 서비스 설정 기반 동적 메뉴 생성
  - 서비스별 섹션 (접힘/펼침)
  - 서비스 아이콘 표시
- **검증**: ibk-chat 섹션이 사이드바에 표시

#### Task 2.3: ibk-chat 서비스 페이지 구조
- **파일**: `apps/frontend-next/src/app/dashboard/services/ibk-chat/`
- **내용**:
  - 레이아웃 (서비스 헤더 + 콘텐츠)
  - 기존 품질분석, 유저분석 페이지 연결
- **검증**: ibk-chat > 품질분석 네비게이션 동작

#### Task 2.4: 서비스별 API 엔드포인트 설계 (백엔드)
- **파일**: `apps/backend/src/service-health/`
- **내용**:
  - 공통 헬스체크 엔드포인트 `/api/services/:serviceId/health`
  - 서비스별 KPI 엔드포인트 `/api/services/:serviceId/kpi`
- **검증**: API 응답 형식 확인

### Phase 3: 검증 후 확장 (판단 후)

#### Task 3.1: 나머지 서비스 카드 연결
- ibk, wind-etl, minkabu-etl 카드 데이터 연결
- 각 서비스 헬스체크 API 연동

#### Task 3.2: 사이드바 전체 재구성
- 기능 중심 메뉴 제거
- 서비스 중심 메뉴로 전환
- 시스템 관리 섹션 유지

#### Task 3.3: 기존 페이지 재배치
- 기존 URL 리다이렉트 설정
- 서비스별 디렉토리로 페이지 이동

## 파일 구조 변경

### Before (현재)
```
apps/frontend-next/src/app/dashboard/
├── page.tsx                  (ibks 고정)
├── business/
├── operations/
├── quality/
├── chatbot-quality/
├── user-analytics/
├── ai-performance/
├── admin/
│   ├── batch-analysis/
│   └── ...
├── etl/
│   ├── wind/
│   └── minkabu/
└── report-monitoring/
```

### After (Phase 3 완료 후)
```
apps/frontend-next/src/app/dashboard/
├── page.tsx                  (서비스 카드 그리드)
├── services/
│   ├── ibk-chat/
│   │   ├── layout.tsx        (서비스 헤더)
│   │   ├── page.tsx          (서비스 홈/개요)
│   │   ├── quality/
│   │   ├── users/
│   │   ├── ai-performance/
│   │   └── batch-analysis/
│   ├── ibk/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── status/
│   │   └── data-loading/
│   ├── wind-etl/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── status/
│   │   └── logs/
│   └── minkabu-etl/
│       ├── ...
└── admin/                    (시스템 관리 유지)
    ├── users/
    ├── roles/
    └── filters/
```

## 컴포넌트 구조

### ServiceCard 컴포넌트
```tsx
// apps/frontend-next/src/components/service-card/ServiceCard.tsx

interface ServiceCardProps {
  config: ServiceConfig;
  data: ServiceHealthData;
  onNavigate: (path: string) => void;
}

export function ServiceCard({ config, data, onNavigate }: ServiceCardProps) {
  return (
    <Card onClick={() => onNavigate(`/dashboard/services/${config.id}`)}>
      <Card.Header>
        <StatusIcon status={data.status} />
        <Card.Title>{config.name}</Card.Title>
        <Card.Description>{config.description}</Card.Description>
      </Card.Header>

      <Card.KPIs>
        {config.card.kpis.map(kpi => (
          <KPIItem
            key={kpi.key}
            label={kpi.label}
            value={data.kpis[kpi.key]}
            format={kpi.format}
            status={getKPIStatus(data.kpis[kpi.key], kpi.thresholds)}
          />
        ))}
      </Card.KPIs>

      <Card.Chart>
        <MiniChart
          type={config.card.chart.type}
          data={data.chartData}
          label={config.card.chart.label}
        />
      </Card.Chart>

      <Card.Footer>
        <Button variant="link">상세 보기 →</Button>
      </Card.Footer>
    </Card>
  );
}
```

## API 설계

### 서비스 헬스 API

```typescript
// GET /api/services/:serviceId/health
interface ServiceHealthResponse {
  serviceId: string;
  status: 'healthy' | 'warning' | 'error';
  statusReason?: string;
  lastChecked: string;
  kpis: Record<string, number | string>;
  chartData: Array<{ timestamp: string; value: number }>;
}

// 예시 응답 (ibk-chat)
{
  "serviceId": "ibk-chat",
  "status": "healthy",
  "lastChecked": "2025-02-05T10:30:00Z",
  "kpis": {
    "activeSessions": 23,
    "resolutionRate": 87.2,
    "avgResponseTime": 1.2
  },
  "chartData": [
    { "timestamp": "10:00", "value": 15 },
    { "timestamp": "10:15", "value": 18 },
    { "timestamp": "10:30", "value": 23 }
  ]
}
```

## 수용 기준 (Acceptance Criteria)

### Phase 1 완료 기준
- [ ] ServiceConfig 타입이 shared-types에 정의됨
- [ ] 4개 서비스 설정이 services.ts에 정의됨
- [ ] ServiceCard 컴포넌트가 mock 데이터로 렌더링됨
- [ ] useServiceHealth 훅이 API 호출 가능

### Phase 2 완료 기준
- [ ] 홈 대시보드에 4개 서비스 카드가 표시됨
- [ ] 각 카드에 상태, KPI 3개, 미니차트가 표시됨
- [ ] 카드 클릭 시 서비스 상세 페이지로 이동
- [ ] 사이드바에 ibk-chat 서비스 섹션이 표시됨
- [ ] ibk-chat 서비스 메뉴에서 기존 페이지로 네비게이션 가능

### Phase 3 완료 기준
- [ ] 모든 서비스가 사이드바에 서비스별 섹션으로 표시됨
- [ ] 기존 기능 중심 메뉴가 서비스 중심으로 재배치됨
- [ ] 모든 기존 URL이 새 구조로 리다이렉트됨
- [ ] 새 서비스 추가 시 설정 파일만 수정하면 됨

## 리스크 및 완화 방안

| 리스크 | 영향 | 완화 방안 |
|--------|------|----------|
| API 응답 형식 불일치 | 카드 렌더링 실패 | 어댑터 패턴으로 데이터 변환 |
| 기존 URL 북마크 깨짐 | 사용자 혼란 | 리다이렉트 설정 |
| 서비스별 KPI가 다름 | 카드 일관성 부족 | ServiceConfig로 커스터마이즈 허용 |
| Phase 2 피드백 부정적 | 전체 계획 수정 필요 | Phase 2에서 빠른 피드백 수집 |

## 일정 (예상)

- **Phase 1**: 1-2일
- **Phase 2**: 2-3일
- **피드백 수집**: 1-2일
- **Phase 3**: 3-5일 (피드백 반영)

**총 예상**: 7-12일 (피드백 반영 포함)

---

*Generated by Prometheus Planning Session*
*Date: 2025-02-05*
